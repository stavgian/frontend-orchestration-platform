{
  "name": "shared-event-bus",
  "root": "libs/shared-event-bus",
  "sourceRoot": "libs/shared-event-bus/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/libs/shared-event-bus"],
      "options": {
        "command": "tsc -p libs/shared-event-bus/tsconfig.lib.json"
      }
    }
  }
}
